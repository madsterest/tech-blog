<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Tech forum for all</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.5.0/css/bootstrap.min.css"
    />
  </head>
  <body>
    <header>
      <nav class=" text-monospace navbar navbar-expand-sm navbar-dark bg-dark p-3 mb-5">
        <div class="container-fluid">
          <span class="navbar-brand mb-0 h1">Hello Users</span>
        </div>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="/">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/dashboard">Dashboard</a>
            </li>
            {{#if logged_in}}
            <li class="nav-item">
              <button type="button" id ="logout-btn" class="btn btn-light">Logout</button>
            </li>
            {{else}}
            <li class="nav-item">
                <a
                href="/login"
                class="btn btn-light"
                role="button"
                aria-pressed="true"
                >Login</a
              >
            </li>
            {{/if}}
          </ul>
        </div>
      </nav>
    </header>
    <main>
      <div class="container-fluid text-monospace">
      {{{ body }}}
      </div>
      </main>

      {{#if logged_in}}
      <script src = "/js/logout.js"></script>
      {{/if}}
  </body>
</html>